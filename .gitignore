# Generated by the pipeline (regenerated every run; published as artifacts / Pages / Releases).
/evidence/
/raw-evidence/
/emass-package/
*.sarif
!policy/**/*.sarif      # (none today, but don't accidentally ignore policy fixtures)
ato-status.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.coverage
coverage.xml
htmlcov/
.pytest_cache/
.ruff_cache/

# Node / misc tooling caches
node_modules/
.cache/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local secrets — NEVER commit (and the secrets gate will catch them anyway)
*.pem
*.key
*.p12
.env
.env.*
